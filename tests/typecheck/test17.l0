type Animal = struct { #name: string };
type Dog = struct { #name: string, #breed: string };

let animalRef: ref<Animal> = box({ #name="Generic" });
let dogRef: ref<Dog> = box({ #name="Buddy", #breed="Golden" });

animalRef := !dogRef;;