let emptyList = nil;
let singletonList = 1 :: emptyList;
let myList = 1 :: 2 :: 3 :: emptyList;

let sum = fn lst: list<int> => {
  match lst {
    nil -> 0
    | head :: tail -> head + sum(tail)
  }
};

sum(myList);;